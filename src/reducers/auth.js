import {
        
    } from "../actions";
  
const defautlState = {};

export default ( state = defautlState, action ) => {
    switch (action.type) {
        default:
            return state;
    }
  };